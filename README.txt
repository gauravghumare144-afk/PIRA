PRIA App — complete updated PWA

Deploy all files in this folder to GitHub Pages, Vercel, Netlify, or another HTTPS host.
Open the deployed site in Chrome/Edge. The visible “📲 Install PRIA App” button triggers the browser install prompt when the site is installable.
The app also registers a service worker for offline support.
